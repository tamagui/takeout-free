import { createV5Theme } from '@tamagui/themes/v5'

// v5 themes - uses createV5Theme for simplified setup
// component themes disabled to reduce CSS output size

export const themes = createV5Theme({
  componentThemes: false,
})
